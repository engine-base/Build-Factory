/**
 * T-V3-C-52 / S-026 — Ticket-mandated path alias for the design_html_editor
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-026-design_html_editor.spec.tsx` (which is where
 * the existing screen-test harness in `frontend/tests/screens/` lives, see
 * S-023 / S-025 / S-033 specs in the same directory). This file is a thin
 * pointer so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-026-design_html_editor.spec.tsx
 */

export {};
