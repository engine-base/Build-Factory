/**
 * T-V3-C-59 / S-029 — Ticket-mandated path alias for the task_dag_view
 * Vitest spec. The canonical spec lives at
 * `frontend/tests/screens/S-029-task_dag_view.spec.tsx` (which is where
 * the existing screen-test harness in `frontend/tests/screens/` lives, see
 * S-025 / S-017 / S-048 specs in the same directory). This file is a thin
 * pointer so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-029-task_dag_view.spec.tsx
 */

export {};
