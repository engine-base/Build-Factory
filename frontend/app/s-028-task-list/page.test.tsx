/**
 * T-V3-C-58 / S-028 — Ticket-mandated path alias for the task_list Vitest spec.
 * The canonical spec is located at
 * `frontend/tests/screens/S-028-task_list.spec.tsx` (which is where the
 * existing screen-test harness in `frontend/tests/screens/` lives, see S-001
 * / S-033 / S-048 specs in the same directory). This file is a thin pointer
 * so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-028-task_list.spec.tsx
 */

export {};
