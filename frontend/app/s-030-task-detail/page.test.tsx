/**
 * T-V3-C-60 / S-030 — Ticket-mandated path alias for the task_detail Vitest
 * spec. The canonical spec is located at
 * `frontend/tests/screens/S-030-task_detail.spec.tsx` (which is where the
 * existing screen-test harness in `frontend/tests/screens/` lives, see S-001
 * / S-028 / S-033 specs in the same directory). This file is a thin pointer
 * so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-030-task_detail.spec.tsx
 */

export {};
