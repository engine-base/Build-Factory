/**
 * T-V3-C-46 / S-020 — Ticket-mandated path alias for the hearing_session
 * Vitest spec. The canonical spec lives at
 * `frontend/tests/screens/S-020-hearing_session.spec.tsx` alongside the
 * existing screen-test harness (see S-001 / S-016 / S-048 specs in the same
 * directory). This file is a thin pointer so anyone navigating to the path
 * declared in `tickets-group-c-ui-part2.json::files_changed[1]` finds the
 * real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-020-hearing_session.spec.tsx
 */

export {};
