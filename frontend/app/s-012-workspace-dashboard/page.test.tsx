/**
 * T-V3-C-61 / S-012 — Ticket-mandated path alias for the workspace_dashboard
 * Vitest spec. The canonical spec lives at
 * `frontend/tests/screens/S-012-workspace_dashboard.spec.tsx` alongside the
 * existing screen-test harness (see S-016 / S-020 specs in the same
 * directory). This file is a thin pointer so anyone navigating to the path
 * declared in `tickets-group-c-ui-part2.json::files_changed[1]` finds the
 * real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-012-workspace_dashboard.spec.tsx
 */

export {};
