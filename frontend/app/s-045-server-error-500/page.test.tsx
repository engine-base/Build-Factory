/**
 * T-V3-C-54 / S-045 — Ticket-mandated path alias for the server_error_500
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-045-server_error_500.spec.tsx` (which is where
 * the existing screen-test harness in `frontend/tests/screens/` lives, see
 * S-048 / S-008 / etc. specs in the same directory). This file is a thin
 * pointer so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-045-server_error_500.spec.tsx
 */

export {};
