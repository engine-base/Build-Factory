/**
 * T-V3-C-64 / S-015 — Ticket-mandated path alias for the workspace_invite
 * Vitest spec. The canonical spec lives at
 * `frontend/tests/screens/S-015-workspace_invite.spec.tsx` alongside the
 * existing screen-test harness (see S-001 / S-028 / S-033 specs in the same
 * directory). This file is a thin pointer so anyone navigating to the path
 * declared in `tickets-group-c-ui-part2.json::files_changed[1]` finds the
 * real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-015-workspace_invite.spec.tsx
 */

export {};
