/**
 * T-V3-C-63 / S-014 — Ticket-mandated path alias for the workspace_members
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-014-workspace_members.spec.tsx` (same convention
 * as the existing S-008 / S-016 / S-028 specs under
 * `frontend/tests/screens/`). This file is a thin pointer so anyone
 * navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-014-workspace_members.spec.tsx
 */

export {};
