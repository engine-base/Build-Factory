/**
 * T-V3-C-56 / S-047 — Ticket-mandated path alias for the maintenance Vitest
 * spec. The canonical spec is located at
 * `frontend/tests/screens/S-047-maintenance.spec.tsx` (alongside the rest of
 * the screen-test harness in `frontend/tests/screens/`, see S-001 / S-008 /
 * S-048 specs). This file is a thin pointer so anyone navigating to the path
 * declared in `tickets-group-c-ui-part2.json::files_changed[1]` finds the
 * real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-047-maintenance.spec.tsx
 */

export {};
