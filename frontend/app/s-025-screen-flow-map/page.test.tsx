/**
 * T-V3-C-51 / S-025 — Ticket-mandated path alias for the screen_flow_map
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-025-screen_flow_map.spec.tsx` (which is where
 * the existing screen-test harness in `frontend/tests/screens/` lives, see
 * S-016 / S-017 / S-048 specs in the same directory). This file is a thin
 * pointer so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-025-screen_flow_map.spec.tsx
 */

export {};
