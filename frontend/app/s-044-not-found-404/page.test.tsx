/**
 * T-V3-C-53 / S-044 — Ticket-mandated path alias for the not_found_404
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-044-not_found_404.spec.tsx` (which is where the
 * existing screen-test harness in `frontend/tests/screens/` lives, see
 * S-048 / S-063 / S-064 specs in the same directory). This file is a thin
 * pointer so anyone navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-044-not_found_404.spec.tsx
 */

export {};
