/**
 * T-V3-C-57-1 / S-027 — Ticket-mandated path alias for the task_kanban
 * Vitest spec. The canonical spec is located at
 * `frontend/tests/screens/S-027-task_kanban.spec.tsx` (same convention as the
 * existing S-016 / S-017 / S-025 / S-048 specs in
 * `frontend/tests/screens/`). This file is a thin pointer so anyone
 * navigating to the path declared in
 * `tickets-group-c-ui-part2.json::files_changed[1]` finds the real spec.
 *
 * Run the actual tests via:
 *   pnpm test -- frontend/tests/screens/S-027-task_kanban.spec.tsx
 */

export {};
