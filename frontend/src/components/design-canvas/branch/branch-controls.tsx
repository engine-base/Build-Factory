// TODO: Build-Factory integration - port Onlook branch controls in a later phase.
type Props = {
    onClose?: () => void;
    branchId?: string;
    branch?: unknown;
};

export const BranchControls = (_props: Props) => null;
