# ヒアリング詳細ログ — 2026-05-01

> Build-Factory プロジェクトの詳細ヒアリング履歴。STEP 1 → STEP 2 → STEP 3 の決定過程を記録。
> サマリは `docs/PROJECT_BRIEF.md` を参照。

---

## ヒアリング対象

- 依頼者: 高本まさと（masato）
- 会社: 株式会社 ENGINE BASE
- 対話相手: Claude（Build-Factory アーキテクト役）
- 利用スキル: `hearing` （ENGINE BASE 製ヒアリングスキル）

---

## STEP 1: プロジェクトの全体像と動機

### 当初の依頼

> ENGINE BASE 内で受託・自社開発を高速化したい開発フロー特化版を作る。company-dashboard と並走で。

### 第1次仮説（Claude 提示）

- 仮説 1: Claude Code 補完仮説（個人ツール → チーム型へ）
- 仮説 2: 持続性・差別化仮説（人間が画面を開かなくても進む）
- 仮説 3: SDLC ナレッジ蓄積仮説（型を資産化）

### まさとさんからの認識修正（重要）

> **「全部 AI」ではない**。設計や要件定義・何を作るかは**人間と決める**こと。
> AI の責務は「実行〜完璧な状態で提供する（手直し・修正がほとんどない）」。

→ 役割分担が明確化:
- 人間: **決める**（要件・設計・物理タスク）
- AI: **補助 + 実行 + 管理**（決めるための完璧補助・決まったことの完璧実行・全ての完璧管理）

### 修正された仮説

- 仮説 1（実装は他に任せる・自分は工場長になる）: Claude Code が実装を引き受ける時代になり、人間ボトルネックは「何を作るか・どう分解するか・終わったか・次は何か」の判断
- 仮説 2（管理盤と工場が一体）: Linear / Notion / Cursor の分断が context loss の元凶。1 OS 統合で引継ぎロスゼロに
- 仮説 3（並行 + 自律）: 1 人で 5〜10 案件並行を AI 社員チーム分業で実現

### Q1〜Q5 への回答（要約）

**Q1. 達成したい状態**:
- 1 人で 10 プロジェクト並行（1 PJ に 1 人監視担当）

**Q2. 案件タイプ**:
- 全タイプ
- 技術スタックは案件ごとに変わる
- ナレッジ + Web 検索で根拠ある選定

**Q3. 自律レベル**:
- ツールとしてはクライアントも進捗が見える
- 実装・テスト後は監視に確認 → クライアントへ進捗共有
- エラー監視も AI 化したい
- ナレッジで AI が育つ

**Q4. プラン B**:
- 実装・テストは Claude Code アプリ側で OK（コスト OK）
- Build-Factory は AI 社員として動く・ブランチ管理・成果物・進め方をツール化

**Q5. タイミング**:
- SaaS サービスとして商用提供したい
- 参考: https://cloud.watch.impress.co.jp/docs/news/2105912.html
- Notion は中身管理・MCP からアクション起動が弱い

### 業界トレンド（WebFetch 結果）

> Okta「Businesses at Work 2026」: 日本で **Notion が 25% 増・初の TOP10 入り**。AI エージェントが「非人間アイデンティティ」として企業に増加（前年比 650%）。
> → AI を社員として組織に組み込む流れは既に来ている。Notion は AI ワークスペースとして成長中だが、**「実装連携 + リーダー AI 壁打ち + 自律品質ループ」の組み合わせは未踏**。

---

## STEP 2: 要件・制約・ステークホルダー

### 第1次提示の F-1〜F-18（Claude 提示）

[STEP 2 で 18 機能要件 + 12 仮説要件を提示]

### まさとさんからの修正（重要）

#### 追加された訴求軸

> 受託案件は増えるがエンジニアが足りない（採用も育成もコスト高）。Build-Factory はここの解決にもなる。

→ moat 補強:「**人を増やさず受託を増やす**」装置

#### F-1 修正: workspace は分離・社員は共有

> workspace 分離するけど社員登録とかは同じでいい（毎回社員を作り直すわけではなく作っているものを）

→ AI 社員 / ナレッジは Account 共通・workspace 横断で兼任

#### F-3 補強: moat の三本柱

リーダー AI 壁打ちループの構成要素:
1. ナレッジ（成功・失敗・ネガティブ）
2. 要件 / 設計 / アーキテクチャスキル
3. クライアントとのすり合わせ（まさとさん経由・AI 秘書と段階的に固める）

#### F-5 補足: タスク分解の粒度

> ここをどういう粒度でやる切り分けるかも大事
> 機能ごともあると思うし、ページごともあるかもしれない
> これは基本実装するための分解（一気にやるとバグ増・最後に繋げやすく）
> ツール内 UI/UX も大切

#### F-13 軽量化: PR レビュー AI

> ローカル起動レベルくらいでいい。基本は Claude Code 側で実装で進めるし。

#### F-14 補強: チェックリスト構造

> F-5（タスク）に紐付けて作る + 別途で最終的に納品状態までのものも必要

#### F-15 簡素化: 監視担当 UI

> Claude Code でやるので基本は進めているものを見るが基本
> F-5 の分解した中の詳細で見れたらいいのでは

#### F-16 大幅誤認識・修正

> プロジェクトごとにワークスペース見るのにどう一気に全てを見るの？
> プロジェクト内に他のプロジェクト進捗とかあるとおかしい

→ 2 階層ダッシュボードへ修正:
- **Account ダッシュボード** = 全 workspace 俯瞰（横断ビュー）
- **Workspace ダッシュボード** = プロジェクト個別のみ

#### F-17 補強: Web 検索

> しっかり組み込んでなるべくしっかり調べるフェーズ入れたい・根拠にもなる

#### チーム規模の制約 更新

> 初めは僕だけど、今後 SaaS とかになると僕ではなくなる
> 他のスタッフにやってもらうこともあるので考慮しておいて

→ ロールベース権限を最初から考慮

### Q-A〜Q-F への回答

**Q-A. MVP タイムライン**: なるべく早く

**Q-B. 試運転案件**: 案件特定するとそれ寄りになるので汎用的に

**Q-C. 仮説要件の優先度**:
- a. 認証・課金 (H-1, H-2): 後回し・ただしアカウント別の閲覧切替は最初から作っておく
- b. AI コスト管理 (H-5): 上限管理設計しておく・自社 API も顧客 API も切替可能・ローカル LLM もできれば嬉しい
- c. テンプレート (H-7): 「どういうこと？」 → Claude が説明 → 「不要・作らない」

**Q-D. 絶対 NG 領域**:
- a. テストカバレッジ漏れ: 基本 NG・ただし厳しすぎて終わらない問題は決めたい
- b. クライアント誤情報送信: NG
- c. その他: Claude が候補提示（API キー漏洩・本番 DB 破壊等 7 項目）

**Q-F. ナレッジ**:
- 学習素材: 差し戻し履歴・監視担当コメント・クライアント評価・チャットでのやりとり
- ナレッジは横断するが教えてはいけないものは出さない
- 専門としての AI 社員ごとに最適化
- 共通ナレッジ + 社員別ナレッジ（既存 company-dashboard と同じ）

### 提示された Claude からの追加提案（採用）

#### 技術スタック選定の 3 段階評価器

```
[L1] 過去案件マッチング (vector 検索 in knowledge_base)
[L2] Web 検索で最新動向取得 (Exa / DuckDuckGo)
[L3] 制約フィルタ (予算 / 期限 / チームスキル / 既存統合)
[アーキテクト AI] が 3 候補を提示・compare artifact で並べる
[監視担当が承認] → 採用ナレッジに記録
[却下] → ネガティブナレッジに記録
```

#### Claude Code 連携の作法（MCP）

```
Build-Factory MCP サーバー公開:
  bf_list_projects() / bf_set_active_project(id)
  bf_get_next_task() / bf_get_spec(task_id) / bf_load_skill(name)
  bf_post_progress(task_id, msg) / bf_attach_artifact(task_id, .)
  bf_request_review(task_id) / bf_get_review_feedback(id)
```

→ Claude Code が「実装メンバー」、Build-Factory が「リーダー / 管理者」の役割分担

#### 失敗時の Plan B（再定義）

| シナリオ | 内容 | プラン B |
|---|---|---|
| A. システム障害 | Build-Factory ダウン | Claude Code 直で開発継続可 |
| B. AI 品質低下 | AI の判断が信頼できない | 監視担当が修正・ナレッジ反映・モデル変更 |
| C. SaaS 化頓挫 | 商用展開できない | 自社利用は継続・ROI 十分 |

#### 赤線リスト 7 項目（採用）

1. 顧客 API キー / 秘密情報の漏洩
2. 本番 DB への破壊的書込み
3. GitHub の force push / 他人ブランチ書換
4. AI コール無限ループ
5. メール / Slack の誤送信
6. ナレッジ横断時の機密情報リーク
7. デプロイ判断の独断

#### テストカバレッジの 3 ターンルール

> 80% 未満は差し戻し・95% 推奨・3 ターン連続改善なしで人間エスカレ

#### コスト管理 5 項目

1. モデル切替（provider・案件・社員・タスク種別ごと）
2. API キー切替（自社 / 顧客持ち込み / ローカル LLM）
3. Token 上限（月次 / 日次 / 1 タスク）
4. コスト可視化
5. 超過時挙動（警告 → 軽量モデル → 停止）

---

## STEP 2 末尾追加: メンバーは workspace 単位

> メンバーなどはプロジェクトごとにするようにして
> どんな権限なのかも変更できる感じにしたい

→ 階層が確定:

```
Account: Owner のみ（課金・AI 社員所有）
Workspaces: Members を個別招待・role はカスタマイズ可
```

---

## STEP 3: 優先順位・開発方針・リスク

[STEP 3 で Must/Should/Could/Won't + MVP + Phase 1-3 + リスク R-1 〜 R-10 提示]

### 既存スキル流用方針（13 個）

```
hearing / requirements-definition / architecture-design / tech-stack
api-design / feature-decomposition / task-decomposition / acceptance-criteria
test-verification / decision-record / release-planning / delivery
verification-loop / skill-creator
```

### 新規スキル候補（4 個・Should レベル）

```
client-comm-bridge        まさとさん ↔ クライアントの会話を AI 秘書同期
code-review-loop          リーダー AI 壁打ちループ専用
claude-code-handoff       Claude Code への仕様パッケージング
progress-rollup           10 案件横断進捗集約
```

---

## STEP 3.5: Claude Code 統合パターン（追加ヒアリング）

### きっかけ

まさとさんから提示された参考記事:
https://qiita.com/nogataka/items/efe8eb9df612d2211221

Anthropic 公式の **「3 エージェント分離パターン」** (Planner / Generator / Evaluator) を Claude Code Sub-agent で実装する記事。

### 記事の要点

- **3 エージェント分離**:
  - Planner: 何を作るか決定 → 実装計画ファイル
  - Generator: どう作るか実装 → コード + git commit
  - Evaluator: 動いているか検証 → PASS/FAIL + フィードバック
- 通信: ファイル経由（メモリ共有なし・コンテキスト独立）
- テスト: Playwright MCP（UI 検証）+ vitest（単体）
- コスト: 6 時間で $200（Opus 4.5 時代）— **ただし Claude Code Pro/Max プラン内で動く**ためサブスク利用
- 段階導入推奨: まず Evaluator 1 体から

### まさとさんの判断

**Q1. パターン**:
> A で進めましょう（Build-Factory が指揮所・Claude Code が実行所）

**Q2. 段階導入のペース**:
> 3 分離でいいのでは？（最初から 3 分離フル展開）

**Q3. コスト上限**:
> 何度も言うけど API ではないでしょこれはプランに入っているので Claude Code やるって言っているよね？
> → コストは Claude Code サブスクリプションプラン内で完結。
> → Build-Factory 側の API コスト管理は OpenAI / Claude API を直接呼ぶ部分のみ。

**Q4. レビュアー AI vs Evaluator の役割**:
> 細かく言うとレビュアー AI（Build-Factory 側）は高位の人ならレビューやテスト・検証のリーダー
> Evaluator は各実装の機能や画面ごとのレビュー
> 全てが終わったら統合して納品のための最大のテストとして全体テストが必要・そこは全体評価・チェック基準あり

→ **階層が確定**:

```
[タスク単位]
  Claude Code 3 分離セット (機能/画面ごと)
    Planner → Generator → Evaluator
    各タスクに評価・デバッグ・テストの通過基準

[全タスク完了後]
  Build-Factory レビュアー AI（高位リーダー）
    全体統合テスト（納品前の最大テスト）
    機能間連携・E2E・パフォーマンス・セキュリティ・納品基準
```

### この決定によって変わったこと

1. **Evaluator は Build-Factory 内ではなく Claude Code Sub-agent として走る**（タスク単位）
2. **Build-Factory レビュアー AI は「Evaluator のリーダー + 統合テスト指揮」**役割に明確化
3. **コスト管理は OpenAI/Claude API を Build-Factory が直接呼ぶ部分のみ**で考える（Claude Code はサブスク内）
4. **段階導入は不採用**（最初から 3 分離フル展開）

---

## 主要な決定とその理由

| # | 決定 | 理由 |
|---|---|---|
| D-1 | company-dashboard を完全コピーして bootstrap | 動く土台を最速で確保 |
| D-2 | Account → Workspace → Member 階層 | SaaS マルチテナント前提 |
| D-3 | Members は workspace 単位 | 案件ごとに招待管理が現実的 |
| D-4 | テンプレート機能なし | 汎用設計で対応・スコープ削減 |
| D-5 | Claude Code は外部実装エンジン（MCP 連携） | 既存ツールの強みを活かす |
| D-6 | port 8001 / 3001 で並走 | 干渉防止 |
| D-7 | DB / Obsidian / credentials 等を完全分離 | データ事故防止 |
| D-8 | 「3 ターン改善なしで人間エスカレ」ルール | カバレッジ無限ループ防止 |
| D-9 | リーダー AI 壁打ちループを moat とする | 他ツールが弱い領域 |
| D-10 | ナレッジは社員別 + 共通の二段構成 | 既存 company-dashboard と同じ |

---

## 未解決事項（次の段階で詰める）

1. MVP のリリース日（具体日付）
2. monthly Token 上限の数値
3. 試運転対象案件の特定
4. チェックリストの粒度
5. リーダー AI の壁打ち回数上限
6. クライアント招待時のメール文面
7. Slack 通知の頻度・チャネル設計
8. Phase 1 の各機能の実装順序

---

## ヒアリングから得られたインサイト

### moat の核心（再確認）

> リーダー AI が下の社員出力を**ナレッジ + スキル + クライアントすり合わせ**を活かして**完璧になるまで壁打ち**する。これがまさとさんが今手作業でやっている品質保証の負担を肩代わりする。

これが Build-Factory の差別化ポイント。Devin / Notion AI / Cursor / Claude Code が誰もやっていない領域。

### 訴求文（SaaS 化時に使える）

- 「**人を増やさず受託を増やす**」
- 「**AI 社員チームで開発フローを丸ごと回す**」
- 「**Claude Code を 10 人分の AI 社員チームに**」

### 業界ポジショニング

- Notion AI: ドキュメント生成
- Linear: タスク管理
- Cursor / Claude Code: 個人実装支援
- Devin: 自律実装
- **Build-Factory**: 上記を統合 + 品質保証 + 複数案件並行管理 + ナレッジで育つ

---

*この記録はヒアリングセッションの完全ログ。サマリは `docs/PROJECT_BRIEF.md`、構造化版は `docs/PROJECT_BRIEF.json`。*
