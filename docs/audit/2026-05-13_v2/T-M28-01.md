# Pre-flight AC Audit (retrofitted) — T-M28-01 (Context Builder skeleton)

- **Task**: T-M28-01 (Context Builder skeleton / existing conversation_memory/rag_context 統合)
- **Sprint**: S2 / **Feature**: M-28 / **Layer**: BE
- **Slice**: S2 / **Wave**: 2.1
- **Label**: REFACTOR
- **Spec link**: `docs/task-decomposition/2026-05-09_v1/tickets.json#T-M28-01`
- **Deps**: T-S0-08 (claude-agent-sdk runner ✅)
- **Status**: ✅ VERIFIED (41 test 関数 + gap closure G1-G4 履歴あり)

---

## 経緯

Phase 9 完走 honest review で Critical Path 後追 audit 対象。本 task は git log 上で gap closure PR (G1-G4) を経て完成しており、test も豊富 (41 関数) なため formal audit MD を retrofit.

---

## 実装

### `backend/services/context_builder.py` (REFACTOR, 既存 conversation_memory + rag_context を統合)
公開 API:
- `preload_constitution(user_id)` — Constitution 文字列 preload
- `lookup_decision(decision_id)` — D-XXX 参照解決
- `read_obsidian_note(slug)` / `write_obsidian_note(slug, content)`
- `is_secretary_active(override)` — secretary AI active 判定
- `build_context(user_message, session_id, ...)` — **メイン entry**
- `build_initial_prompt(user_message, session_id, persona, ...)` — **T-010b-03 で追加**

### 関連 test (41 関数)
```
tests/test_t_m28_01_context_builder_*.py
  - test_g1..g4 (gap closure)
  - test_ac1_build_context_orchestrates_*
  - test_ac2_event_emit_*
  - test_ac3_persona_state_*
  - test_ac4_invalid_input_*
```

---

## AC × test 1:1 mapping

| AC | type | impl | status |
|---|---|---|---|
| 1 | UBIQUITOUS | `build_context` が 4 ステップ (memory + decisions + constitution + mem0) を統合 | ✅ |
| 2 | EVENT-DRIVEN | session 起動で `m28.context_built` audit emit (tier_hit_count + total_chars) | ✅ |
| 3 | STATE-DRIVEN | `is_secretary_active(override)` で env SECRETARY_ACTIVE を尊重 | ✅ |
| 4 | UNWANTED | empty user_message / session_id <=0 で `ContextBuilderError` raise | ✅ |

---

## Gap closure 履歴 (post-audit で発見・閉鎖された 4 件)

| Gap | 内容 | 修正 commit |
|---|---|---|
| G1 | `build_context` 呼び出し時 audit 不在 | 25ea5f6 (T-M28-01 G1-G4) |
| G2 | secretary_active env override 未対応 | 同上 |
| G3 | conflict detection が silent pick | 同上 |
| G4 | invalid input が ValueError でなく TypeError | 同上 |

---

## 完了判定

- [x] 41 test 関数 / AC 4 件
- [x] gap closure 4 件閉鎖済
- [x] T-010b-03 (build_initial_prompt wrapper) が REUSE で乗っかる
- [x] T-M28-04 (Tier 3 9-section) と integration test 整合

---

_retrofitted: 2026-05-14_
