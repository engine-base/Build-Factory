# Pre-flight AC Audit — T-012-01 (red_lines 5 default categories seed)

- **Task**: T-012-01 (red_lines DDL + 5 主要 category seed)
- **Sprint**: 5 / **Feature**: F-012 (赤線リスト + 自動停止) / **Layer**: DB
- **Slice**: S6 / **Wave**: 6.1
- **Label**: NEW (seed-only — テーブル本体は既存 20260512000000_impl_integration_ops_tables.sql)
- **Spec link**: `docs/task-decomposition/2026-05-09_v1/tickets.json#T-012-01`
- **Deps**: T-026-01 (constitutions DDL ✅) + 既存 red_lines テーブル
- **Status**: ✅ VERIFIED (10 spec tests + db_integration regression PASS)

---

## 重要な発見と修正

**初版実装での誤認**: 私が新規に `CREATE TABLE red_lines` を書こうとした結果、既存
`supabase/migrations/20260512000000_impl_integration_ops_tables.sql` で既に
定義されていた `red_lines` テーブル (rule_key 中心 schema) と **重複** し、
`test_t_001_11_db_integration::test_ac4_no_duplicate_table_definitions` が
fail した。

**修正**: 新規 migration はテーブル定義を再宣言せず、**5 default categories の
seed のみ** に変更。既存テーブル schema (rule_key + pattern + severity +
UNIQUE(workspace_id, rule_key)) を活用し、`ON CONFLICT DO NOTHING` で冪等性を担保。

---

## 既存 red_lines テーブル (20260512000000) の schema

```sql
CREATE TABLE IF NOT EXISTS red_lines (
    id              BIGSERIAL PRIMARY KEY,
    workspace_id    BIGINT REFERENCES workspaces(id) ON DELETE CASCADE,
    constitution_id BIGINT REFERENCES bf_constitutions(id) ON DELETE CASCADE,
    rule_key        TEXT NOT NULL,
    pattern         TEXT NOT NULL,
    severity        TEXT NOT NULL DEFAULT 'block' CHECK (severity IN ('block','warn','log')),
    description     TEXT,
    is_enabled      BOOLEAN NOT NULL DEFAULT TRUE,
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    CONSTRAINT uq_red_line UNIQUE (workspace_id, rule_key)
);
```

## 本タスクが追加する seed

`supabase/migrations/20260514000000_red_lines_table.sql` (本 PR):

5 INSERT 行 + ON CONFLICT DO NOTHING + post-seed DO $$ invariant check.

| # | rule_key | severity | pattern (regex) | spec source |
|---|---|---|---|---|
| 1 | `api_key_leak` | block | `sk-ant-... \| sb_(publishable\|secret)_... \| ghp_...` | CLAUDE.md §5.4 |
| 2 | `db_destructive` | block | `DROP\|TRUNCATE\|DELETE *;` | CLAUDE.md §5.4 |
| 3 | `force_push` | warn | `git push --force\|-f\|--no-verify` | CLAUDE.md §5.4 |
| 4 | `infinite_loop` | warn | `while True\|for(;;)` | features.json F-012 |
| 5 | `deploy_decision` | warn | `deploy\|release (prod\|production)` | features.json F-012 |

---

## AC × test 1:1 対応

### AC-1 UBIQUITOUS: 5 default categories seed

| # | spec sub-clause | impl | test | status |
|---|---|---|---|---|
| 1.1 | seed migration ファイル | `supabase/migrations/20260514000000_red_lines_table.sql` | `test_ac1_seed_migration_exists` | ✅ |
| 1.2 | テーブル再宣言しない | INSERT のみ | `test_ac1_no_duplicate_table_create` | ✅ |
| 1.3 | 5 categories seed | migration body | `test_ac1_5_default_categories_seeded` | ✅ |
| 1.4 | global seed (workspace_id=NULL) | INSERT 行頭 NULL | `test_ac1_global_seed_workspace_id_null` | ✅ |
| 1.5 | red_line_detector.py と 1:1 | DEFAULT_CATEGORIES | `test_ac1_categories_match_red_line_detector_constants` | ✅ |

### AC-2 EVENT-DRIVEN: audit_logs trigger 連携 (既存)

| # | spec sub-clause | impl | test | status |
|---|---|---|---|---|
| 2.1 | created_at 経由で audit_logs trigger 対応 | 既存テーブルに created_at あり | `test_ac2_existing_table_has_audit_trigger_compatibility` | ✅ |

### AC-3 STATE-DRIVEN: 冪等性 (ON CONFLICT)

| # | spec sub-clause | impl | test | status |
|---|---|---|---|---|
| 3.1 | ON CONFLICT DO NOTHING | INSERT 末尾 | `test_ac3_seed_idempotent_on_conflict` | ✅ |
| 3.2 | post-seed invariant 検証 | DO $$ RAISE EXCEPTION | `test_ac3_post_seed_invariant_check` | ✅ |

### AC-4 UNWANTED: invalid value 検出 (既存 CHECK)

| # | spec sub-clause | impl | test | status |
|---|---|---|---|---|
| 4.1 | severity CHECK ('block','warn','log') | 既存テーブル CHECK | `test_ac4_severity_values_are_block_warn_log_only` | ✅ |
| 4.2 | seed に hardcoded secret 無し | regex のみ / literal なし | `test_ac4_no_hardcoded_secrets_in_seed` | ✅ |

---

## 完了判定

- [x] seed migration が既存 schema と整合
- [x] AC 4 件すべてに 1:1 test (10 関数)
- [x] `pytest test_t_012_01_red_lines_ddl.py` = 10 PASS
- [x] `pytest test_t_001_11_db_integration` (重複 CREATE 検査) = PASS
- [x] `bash scripts/lint-mock.sh` = 16/16 OK
