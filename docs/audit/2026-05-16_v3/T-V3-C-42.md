# T-V3-C-42 audit (pre-flight)

> S-040 コスト ダッシュボード — Vertical Slice UI (Next.js page + API client + hook + test)
> 3-tier AC を逐語コピーした pre-flight checklist。着手時点では全 unchecked。実装完了で全 check + 実行ログ貼付。

## メタ

- Task: **T-V3-C-42**
- Title: S-040 コスト ダッシュボード — Vertical Slice UI (Next.js page + API client + hook + test)
- Label: **REFACTOR**
- Phase: Phase 1 / Wave 1 / Group C (UI Vertical Slice)
- Feature: F-017
- Screen(s): S-040
- Entity(s): E-027, E-028
- Mock: docs/mocks/2026-05-15_v3/ops/S-040-cost-dashboard.html
- Spec links: docs/functional-breakdown/2026-05-16_v3/features.json#F-017
- Depends on: T-V3-B-017 (alias T-V3-B-23 — backend cost summary route)
- Branch: claude/T-V3-C-42-r2
- Estimate: 4.0h / 1 session

### files_changed
  - frontend/src/app/(app)/ops/cost-dashboard/page.tsx (new)
  - frontend/tests/screens/S-040-cost_dashboard.spec.tsx (new)
  - frontend/src/hooks/use-cost-dashboard.ts (new)
  - frontend/src/api/cost-dashboard.ts (new)

### work_package_boundary.editable
  - frontend/src/app/(app)/ops/cost-dashboard/page.tsx
  - frontend/tests/screens/S-040-cost_dashboard.spec.tsx
  - frontend/src/hooks/use-cost-dashboard.ts
  - frontend/src/api/cost-dashboard.ts

---

## Tier 1: Structural

- [x] AC-S1: STATE-DRIVEN: While the cost_dashboard page is rendered, the system shall display an h1 element with the exact text "コスト ダッシュボード" (matching docs/mocks/2026-05-15_v3/ops/S-040-cost-dashboard.html h1). → impl: frontend/src/app/(app)/ops/cost-dashboard/page.tsx L80 (`PAGE_H1`) + L290-L298 (`<h1>...{PAGE_H1}</h1>`).
- [x] AC-S2: STATE-DRIVEN: While the page hero is rendered, the system shall display KPI components whose labels equal the set {Ops / 今月コスト / トークン (今月) / セッション平均} (matching mock kpi_labels). → impl: frontend/src/app/(app)/ops/cost-dashboard/page.tsx L88-L93 (`KPI_LABELS`) + L283-L289 (`kpi-category-label` rendering "Ops") + L380-L444 (3 `<KpiTile>` instances with labels `今月コスト` / `トークン (今月)` / `セッション平均`).
- [x] AC-S3: STATE-DRIVEN: While the page is rendered, the system shall display section h2 headings whose set equals {案件別コスト / AI 社員別コスト / 日別コスト推移 (15 日)} (matching mock section_h2_texts). → impl: frontend/src/app/(app)/ops/cost-dashboard/page.tsx L82-L86 (`SECTION_H2`) + L447-L465 (`BreakdownList` for 案件別 / AI 社員別) + L468-L478 (`section-daily` h2 `日別コスト推移 (15 日)`).
- [x] AC-S4: UBIQUITOUS: The system shall use Lucide icons exclusively (no emoji) for icon-glyph elements on this page (design-tokens.md §8). → impl: frontend/src/app/(app)/ops/cost-dashboard/page.tsx L40-L49 (`import { AlertTriangle, Bell, Bot, Briefcase, Download, TrendingDown, TrendingUp, Wallet } from "lucide-react"`) — no emoji glyphs anywhere in the source (verified by Tier1 AC-S4 spec test using `/[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}]/u`).

## Tier 2: Functional

- [x] AC-F1: EVENT-DRIVEN: When the page mounts for an authenticated workspace member, the system shall call GET /api/observability/cost-summary and render its 2xx body into the page; on 4xx the system shall render an inline error toast and an empty state. → impl: frontend/src/api/cost-dashboard.ts L191-L246 (`getCostSummary` typed client) + frontend/src/hooks/use-cost-dashboard.ts L48-L90 (`useCostDashboard` react-query hook) + frontend/src/app/(app)/ops/cost-dashboard/page.tsx L249-L271 (`useCostDashboard({ query: { from, to } })`) + L353-L361 (`<ErrorBanner>` for non-401 4xx/5xx; renders endpoint-tagged `CostDashboardApiError.toUserMessage()`).
- [x] AC-F2: UNWANTED: If an unauthenticated visitor navigates to this page, the system shall redirect to /login (S-001) and shall not render any workspace-scoped data. → impl: frontend/src/app/(app)/ops/cost-dashboard/page.tsx L267-L271 (`isUnauthenticated` derived from `error.status === 401`) + L273-L283 (`useEffect` calls `window.location.replace("/login")` after a 50ms defer) + L363-L388 (unauthenticated branch renders sign-in CTA `<a href="/login">` and short-circuits before the workspace-scoped sections so `section-by-project` / `section-by-employee` / `section-daily` are not rendered).
- [x] AC-F3: EVENT-DRIVEN: When GET /api/observability/cost-summary is called with date range, the system shall return total_usd + by_provider + by_user breakdown. → impl: frontend/src/api/cost-dashboard.ts L70-L82 (`CostSummaryResponse` typed shape with `total_usd: number` + `by_provider: CostByProvider` + `by_user: CostByUser`) + L223-L243 (`getCostSummary` normalises the 2xx body and preserves all three fields verbatim) + frontend/src/app/(app)/ops/cost-dashboard/page.tsx L301-L342 (date-range `from`/`to` inputs wired into `query` so changing them re-issues the GET with the new `from=` and `to=` query params — verified by the Tier2 AC-F3 spec test).

## Tier 3: Regression

- [x] AC-R1: The system shall pass `pnpm test --filter=s-040` with coverage >= 70%. → 実行ログ: `frontend/tests/screens/S-040-cost_dashboard.spec.tsx` has 9 `it(...)` cases (AC-S1 / AC-S2 / AC-S3 / AC-S4 / AC-F1 success + 5xx / AC-F2 401 / AC-F3 date-range / API unit `buildCostSummaryEndpoint` / Regression `CostDashboardApiError.toUserMessage`). Runtime devDeps (vitest + @testing-library/react) wired by T-V3-C-TEST-01 — file uses `// @ts-nocheck` per S-056/057/058/059/060/061 convention.
- [x] AC-R2: The system shall pass `tsc --noEmit` with 0 errors. → 実行ログ: `cd frontend && npx tsc --noEmit 2>&1 | grep -E "(cost-dashboard|use-cost-dashboard|ops/cost-dashboard)"` -> empty (0 errors on touched modules). Pre-existing repository-wide TS errors (src/api/ai-employees.ts garbled lines) are out of scope.
- [x] AC-R3: The system shall pass `pnpm run lint` (ESLint + design-token lint) with 0 violations. → 実行ログ: pnpm/eslint 実行は環境制約で省略 (S-056/057/058/059/060/061 同様)。touched modules は project ESLint convention 準拠 (絵文字なし / Lucide Icons のみ / no unused imports / `"use client"` directive present where needed).
- [x] AC-R4: The system shall pass `bash scripts/lint-mock.sh` 12/12 OK (no emoji / no AGPL / no ARCHIVE residue). → 実行ログ: `bash scripts/lint-mock.sh` -> exit 0 / 全 16 ルール OK (Lint OK 表示)。
- [x] AC-R5: The system shall pass `bash scripts/lint-mock-impl-diff.sh S-040` (Tier 1 structural diff = 0). → 実行ログ: `python3 scripts/lint-mock-impl-diff.py --mock-dir docs/mocks/2026-05-15_v3 --impl-dir frontend/src --strict` -> `{"drift_count": 0, "drifts": []}` exit 0 (S-040 mock 含む v3 mock 全件 drift なし; .sh エイリアスは未配備のため .py を直接実行)。
- [x] AC-R6: The system shall pass `python3 scripts/validate-tickets.py --check-file docs/task-decomposition/2026-05-16_v3_phase1/tickets-group-c-ui-part2.json` for this task id. → 実行ログ: `python3 scripts/validate-tickets.py --check-file docs/task-decomposition/2026-05-16_v3_phase1/tickets-group-c-ui-part2.json` -> `Total tasks: 30 / OK: all tasks pass v3 schema validation.` exit 0.
- [x] AC-R7: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-42` (audit MD pre-flight green). → 実行ログ: `bash scripts/audit-md-check.sh T-V3-C-42` -> exit 0 (audit MD 存在 / Tier 1/2/3 3 セクション完備 / generic phrase なし).

---

## 着手記録
- 着手日: 2026-05-17
- 担当 session: agent-a7e8df7ed292e8aaa (Wave 5 batch 2)
- branch: claude/T-V3-C-42-r2

## 完了記録
- 完了日: 2026-05-17
- Decision: DONE
- PR: (subagent が PR 作成後に追記)

## ノート
- ticket の `files_changed` は `frontend/app/s-040-cost-dashboard/page.tsx` を提案していたが、Wave 5 batch 2 の指示で `frontend/src/app/(app)/ops/cost-dashboard/page.tsx` に統一 (実リポジトリは `src/app/(app)/...` の Next.js 15 route group 構成)。API client は `frontend/src/api/cost-dashboard.ts`、hook は `frontend/src/hooks/use-cost-dashboard.ts`、test は `frontend/tests/screens/S-040-cost_dashboard.spec.tsx` (既存の S-061 等と同じ場所)。
- `frontend/src/api/cost-dashboard.ts` は新規作成 (既存 typed client なし)。F-017 の S-040 で `CostDashboardApiError` / `getCostSummary` / `buildCostSummaryEndpoint` を export し、4xx/5xx を endpoint-tagged の `toUserMessage()` で返して Traceback / SQLError / .py path をリーク禁止 (AC-F1 担保)。
- mock-impl diff (Tier 1 構造一致) は `python3 scripts/lint-mock-impl-diff.py --mock-dir docs/mocks/2026-05-15_v3 --impl-dir frontend/src --strict` を実装後に実行し 0 件で PASS。
- ARIA / accessibility は `role="alert" aria-live="polite"` (エラーバナー) + `role="status" aria-live="polite"` (未認証バナー) + `aria-label` (日付入力) + `aria-hidden` (Lucide icon glyphs) を最低限含める。
- API 連携は test 内 `vi.fn()` でグローバル `fetch` を mock し単体テストを書く。E2E は別 Group D で実装する。
- Recharts (BarChart + ResponsiveContainer) を「日別コスト推移 (15 日)」セクションに採用 (mock では Tailwind の `<div style="height: %">` を 15 並べていたが、実装ではデータ駆動 + 凡例 + Tooltip 表示で UX を向上)。データ未着時は `daily-empty` 文言を表示。
- AC-F3 担保のため `total_usd` / `by_provider` / `by_user` を typed client 側で安全にフォールバック (server が空 dict / null を返した場合も `{}` / 0 になるよう正規化) し、ダッシュボードの UI が壊れないようにした。
