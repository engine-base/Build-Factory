# T-V3-C-13 audit

> S-037 AI 社員 詳細 画面実装 (Vertical Slice / UI)
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-037
- feature_id: F-003  (related: F-003, F-022)
- entity_ids: E-034, E-035, E-036
- mock_path: docs/mocks/2026-05-15_v3/ai/S-037-ai-employee-detail.html
- depends_on: T-FOUNDATION-08, T-V3-B-AI-01, T-V3-B-AI-02
- branch: claude/T-V3-C-13

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-037 page is rendered, the system shall include a `data-screen-id="S-037"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-037 page is rendered, the system shall display an h1 element with text matching screens.json[S-037].h1_text ("devon (Senior Dev)"). → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S3: STATE-DRIVEN: While S-037 page is rendered, the system shall render h2 section headings matching screens.json[S-037].section_h2_texts (cap 12): Persona / System Prompt / スキル (8) / 実行履歴 (今月 87 件). → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-037 page performs its primary action, the system shall call GET /api/ai-employees/{id} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: EVENT-DRIVEN: When the S-037 page performs its primary action, the system shall call PUT /api/ai-employees/{id} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When the S-037 page performs its primary action, the system shall call POST /api/ai-employees/{id}/test via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: UNWANTED: If a backing API call from S-037 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F5: STATE-DRIVEN: While clone opt-in is FALSE for the source user, the system shall return 403 for POST /api/ai-employees/{id}/clone-from-user. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F6: UNWANTED: If POST /api/ai-employees/{id}/test is called more than 20 times per minute per workspace, the system shall return 429. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-13 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-13. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-037` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-13` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-13

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
