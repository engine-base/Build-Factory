# T-V3-C-06 audit

> S-006 10 案件 俯瞰 画面実装 (Vertical Slice / UI) +dialog S-054
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-006 / S-054
- feature_id: F-024  (related: F-024, F-018, F-008, F-007, F-017)
- entity_ids: E-008, E-009, E-018, E-025, E-027, E-029
- mock_path: docs/mocks/2026-05-15_v3/account/S-006-account-dashboard.html
- depends_on: T-FOUNDATION-08, T-V3-B-SEARCH-01, T-V3-B-NOTIF-01, T-V3-B-AUDIT-01, T-V3-B-PHASE-01, T-V3-B-TASK-01, T-V3-B-COST-01
- branch: claude/T-V3-C-06

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-006 page is rendered, the system shall include a `data-screen-id="S-006"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-006 page is rendered, the system shall display an h1 element with text matching screens.json[S-006].h1_text ("10 案件 俯瞰"). → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S3: STATE-DRIVEN: While S-006 page is rendered, the system shall render h2 section headings matching screens.json[S-006].section_h2_texts (cap 12): Pending Reviews / Phase 進捗 / 完了タスク (7d) / 全 Workspaces (10 案件並走中) / AI 社員 使用率（今週） / 直近の Activity. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S4: UBIQUITOUS: The system shall expose KPI labels on S-006 matching screens.json[S-006].kpi_labels: Active Projects / Running Sessions / Monthly Cost / Anomalies (24h). → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-006 page performs its primary action, the system shall call GET /api/accounts/{id}/dashboard via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: UNWANTED: If a backing API call from S-006 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When GET /api/accounts/{id}/dashboard is called, the system shall aggregate KPI across all workspaces the caller belongs to within the account. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: EVENT-DRIVEN: When the server returns 401 with session_expired code, the system shall show the session_expired dialog (S-054 pattern) and preserve in-flight form data in localStorage. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-06 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-06. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-006 S-054` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-06` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-06

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
