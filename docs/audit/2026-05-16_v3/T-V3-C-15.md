# T-V3-C-15 audit

> S-042 クライアントポータル 画面実装 (Vertical Slice / UI)
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-042
- feature_id: F-013  (related: F-013, F-021)
- entity_ids: E-009, E-021
- mock_path: docs/mocks/2026-05-15_v3/client/S-042-client-workspace.html
- depends_on: T-FOUNDATION-08, T-V3-B-PR-01, T-V3-B-RBAC-01
- branch: claude/T-V3-C-15

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-042 page is rendered, the system shall include a `data-screen-id="S-042"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-042 page is rendered, the system shall display an h1 element with text matching screens.json[S-042].h1_text ("案件進捗状況"). → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S3: STATE-DRIVEN: While S-042 page is rendered, the system shall render h2 section headings matching screens.json[S-042].section_h2_texts (cap 12): Phase 2: 統合テスト + 受入 / 最近の更新 / 担当チーム / 納期 / フェーズ進捗. → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-042 page performs its primary action, the system shall call GET /api/client/workspaces/{token} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: EVENT-DRIVEN: When the S-042 page performs its primary action, the system shall call GET /api/client/workspaces/{token}/spec via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When the S-042 page performs its primary action, the system shall call POST /api/client/comments via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: UNWANTED: If a backing API call from S-042 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F5: STATE-DRIVEN: While a client token has expired, the system shall return 409 for GET /api/client/workspaces/{token}. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F6: UNWANTED: If POST /api/client/comments exceeds 20 requests per hour per token, the system shall return 429. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-15 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-15. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-042` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-15` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-15

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
