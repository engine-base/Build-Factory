# T-V3-C-09 audit

> S-009 プロフィール設定 画面実装 (Vertical Slice / UI)
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-009
- feature_id: F-022  (related: F-022, F-023)
- entity_ids: E-001, E-010, E-040
- mock_path: docs/mocks/2026-05-15_v3/account/S-009-profile-settings.html
- depends_on: T-FOUNDATION-08, T-V3-B-AI-02, T-V3-B-PROFILE-01
- branch: claude/T-V3-C-09

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-009 page is rendered, the system shall include a `data-screen-id="S-009"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-009 page is rendered, the system shall display an h1 element with text matching screens.json[S-009].h1_text ("プロフィール設定"). → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S3: STATE-DRIVEN: While S-009 page is rendered, the system shall render h2 section headings matching screens.json[S-009].section_h2_texts (cap 12): プロフィール / 通知設定 / LLM プロバイダ (BYOK) / OAuth 連携 / ユーザークローン (高本さんの判断基準を学習) / Danger Zone. → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-009 page performs its primary action, the system shall call GET /api/me via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: EVENT-DRIVEN: When the S-009 page performs its primary action, the system shall call PUT /api/me via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When the S-009 page performs its primary action, the system shall call POST /api/me/api-keys via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: EVENT-DRIVEN: When the S-009 page performs its primary action, the system shall call DELETE /api/me/oauth/{provider} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F5: UNWANTED: If a backing API call from S-009 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F6: STATE-DRIVEN: While clone_opt_in is FALSE for a user, the system shall not INSERT into user_interaction_log for that user (DB trigger enforced). → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F7: EVENT-DRIVEN: When a user opts out of cloning, the system shall offer immediate deletion of user_knowledge_namespace + user_interaction_log. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-09 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-09. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-009` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-09` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-09

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
