# T-V3-C-14 audit

> S-038 スキルマネージャ 画面実装 (Vertical Slice / UI)
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-038
- feature_id: F-002  (related: F-002, F-003)
- entity_ids: E-035
- mock_path: docs/mocks/2026-05-15_v3/ai/S-038-skill-manager.html
- depends_on: T-FOUNDATION-08, T-V3-B-SKILLS-01, T-V3-B-AI-01
- branch: claude/T-V3-C-14

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-038 page is rendered, the system shall include a `data-screen-id="S-038"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-038 page is rendered, the system shall display an h1 element with text matching screens.json[S-038].h1_text ("スキルマネージャ"). → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-038 page performs its primary action, the system shall call GET /api/skills via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: EVENT-DRIVEN: When the S-038 page performs its primary action, the system shall call POST /api/skills via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When the S-038 page performs its primary action, the system shall call POST /api/skills/{id}/test via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: EVENT-DRIVEN: When the S-038 page performs its primary action, the system shall call POST /api/skills/{id}/archive via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F5: UNWANTED: If a backing API call from S-038 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F6: EVENT-DRIVEN: When GET /api/skills is called with category=ai, the system shall return all non-archived skills matching that category. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F7: EVENT-DRIVEN: When POST /api/skills/{id}/archive is called by an account_owner, the system shall set archived_at and exclude the skill from AI invocation lists. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F8: UNWANTED: If POST /api/skills is called by a non-owner role, the system shall return 403. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F9: UNWANTED: If POST /api/skills/{id}/test is invoked more than 10 times per minute per user, the system shall return 429. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-14 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-14. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-038` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-14` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-14

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
