# T-V3-C-11 audit

> S-011 グローバル検索 (Cmd+K) 画面実装 (Vertical Slice / UI)
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-011
- feature_id: F-024  (related: F-024)
- entity_ids: E-001, E-008, E-009, E-018, E-022, E-029
- mock_path: docs/mocks/2026-05-15_v3/account/S-011-global-search.html
- depends_on: T-FOUNDATION-08, T-V3-B-SEARCH-01
- branch: claude/T-V3-C-11

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-011 page is rendered, the system shall include a `data-screen-id="S-011"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-011 page performs its primary action, the system shall call GET /api/search?q={query}&category={cat} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: UNWANTED: If a backing API call from S-011 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When GET /api/search is called with a non-empty q, the system shall return hits ranked by combined FTS + vector similarity score. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: UBIQUITOUS: The system shall apply RLS so search hits include only resources visible to the caller. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-11 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-11. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-11` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-11

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
