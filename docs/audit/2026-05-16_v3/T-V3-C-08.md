# T-V3-C-08 audit

> S-008 メンバー管理 画面実装 (Vertical Slice / UI) +dialog S-051
> 3-tier AC を逐語コピーし、impl line と実行ログを記録する **pre-flight template**。
> 着手前にこの MD を埋めること (auto-generate は禁止 / generic 文言は CI gate で reject)。

## メタ

- screen(s): S-008 / S-051
- feature_id: F-004  (related: F-004, F-021)
- entity_ids: E-001, E-002, E-043
- mock_path: docs/mocks/2026-05-15_v3/account/S-008-account-members.html
- depends_on: T-FOUNDATION-08, T-V3-B-ACCOUNT-01, T-V3-B-RBAC-01
- branch: claude/T-V3-C-08

## Tier 1: Structural

- [ ] AC-S1: STATE-DRIVEN: While S-008 page is rendered, the system shall include a `data-screen-id="S-008"` attribute on the root element. → impl: <page.tsx 行範囲を着手時に追記>
- [ ] AC-S2: STATE-DRIVEN: While S-008 page is rendered, the system shall display an h1 element with text matching screens.json[S-008].h1_text ("メンバー管理"). → impl: <page.tsx 行範囲を着手時に追記>

## Tier 2: Functional

- [ ] AC-F1: EVENT-DRIVEN: When the S-008 page performs its primary action, the system shall call GET /api/accounts/{id}/members via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F2: EVENT-DRIVEN: When the S-008 page performs its primary action, the system shall call POST /api/accounts/{id}/invitations via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F3: EVENT-DRIVEN: When the S-008 page performs its primary action, the system shall call DELETE /api/accounts/{id}/members/{user_id} via the typed API client. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F4: UNWANTED: If a backing API call from S-008 returns 4xx or 5xx, the system shall surface a non-technical error toast referencing the failing endpoint without leaking server stack traces. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F5: EVENT-DRIVEN: When POST /api/accounts/{id}/invitations is called more than 20 times per hour for the same account, the system shall return 429. → impl: <該当 hook / mutation 行範囲を着手時に追記>
- [ ] AC-F6: EVENT-DRIVEN: When the user requests a destructive action, the system shall show the confirm_delete dialog (S-051 pattern) requiring typed-name confirmation before submitting. → impl: <該当 hook / mutation 行範囲を着手時に追記>

## Tier 3: Regression

- [ ] AC-R1: The system shall pass `vitest run frontend/tests/screens/` covering the new tests for T-V3-C-08 with >= 5 test cases PASS. → 実行ログを着手時に貼付
- [ ] AC-R2: The system shall pass `cd frontend && tsc --noEmit` with 0 errors on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R3: The system shall pass `cd frontend && pnpm run lint` with 0 warnings on touched modules. → 実行ログを着手時に貼付
- [ ] AC-R4: The system shall maintain coverage >= 70% on files changed by T-V3-C-08. → 実行ログを着手時に貼付
- [ ] AC-R5: The system shall pass `bash scripts/lint-mock.sh` with all 16+ rules OK. → 実行ログを着手時に貼付
- [ ] AC-R6: The system shall pass `python3 scripts/lint-mock-impl-diff.py --strict S-008 S-051` (Gate #8 / mock-impl structural diff). → 実行ログを着手時に貼付
- [ ] AC-R7: The system shall pass `python3 scripts/validate-tickets.py` with no errors for this task entry. → 実行ログを着手時に貼付
- [ ] AC-R8: The system shall pass `bash scripts/audit-md-check.sh T-V3-C-08` (audit MD exists, 3 sections present, no generic phrase). → 実行ログを着手時に貼付

### 凡例

- [ ] = 未着手
- [x] = PASS (実行ログ貼付済)
- [/] = SKIP-WITH-REASON (新規 regression ではなく pre-existing baseline drift)
- [!] = FAIL (修正必要)

## 着手記録

- 着手日: (着手時に追記)
- 担当 session: (worktree-id を追記)
- branch: claude/T-V3-C-08

## 完了記録

- 完了日: (完了時に追記)
- Decision: TBD (DONE | BLOCKED | GAP)
- PR: (subagent が PR 作成後に追記)

## ノート

- (実装中の気付きを追記)
