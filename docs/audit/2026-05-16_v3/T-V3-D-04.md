# T-V3-D-04 audit (Pre-flight)

- **Task**: T-V3-D-04 — Twin tables ARCHIVE batch (E-014 legacy tasks / E-007 ai_employee_config / E-027 pull_requests / E-032 repos): 二重実装 4 件解消
- **Group**: D (Drift fix) / **Phase**: Phase 1 末尾 / **Wave**: 4
- **Category**: cleanup / **Label**: ARCHIVE / **Layer**: polish
- **Feature**: —
- **Entity ids**: E-014 Task (legacy), E-007 AIEmployee (legacy ai_employee_config), E-027 PR (legacy pull_requests), E-032 GithubRepo (legacy repos)
- **Screen ids**: —
- **Branch**: `claude/T-V3-D-04-r2`
- **Status legend**: ⬜ PLANNED → 🟡 IMPL_DONE → 🟢 TEST_PASS → ✅ VERIFIED

## Spec links

- docs/functional-breakdown/2026-05-16_v3/entity-drift-summary.md#2-critical-drift-3-件
- docs/functional-breakdown/2026-05-16_v3/entity-drift-summary.md#6-推奨対応-下流-task-decomposition-への流し込み
- docs/decisions/ADR-015-legacy-twin-table-archive.md

## Tier 1: Structural

(該当なし / backend / cleanup / infra タスクのため structural AC は省略)

## Tier 2: Functional

EARS verbatim copy (改変禁止). 各 AC に impl `<file>:<lines>` を埋める.

- [x] AC-F1: UBIQUITOUS: The migration shall mark 4 legacy tables (tasks, ai_employee_config, pull_requests, repos) as ARCHIVE by renaming them to `_archived_<name>` (not DROP) so that audit history is preserved.
  - impl: supabase/migrations/20260516140000_archive_legacy_twins.sql:175-217 — `pairs` 配列 (4 行) + `ALTER TABLE %I RENAME TO %I` (203 行) + idempotent skip check (191-200) + COMMENT ON TABLE archive marker (208-211).
- [x] AC-F2: EVENT-DRIVEN: When a query references one of the 4 legacy tables via the deleted router/model paths, the system shall return HTTP 410 Gone via the modern equivalent endpoint (bf_tasks / ai_employees / prs / github_repos).
  - impl: 4 legacy router (backend/routers/legacy_tasks.py / legacy_pull_requests.py / legacy_repos.py / legacy_ai_employee_config.py) と backend/app/models/legacy/ ディレクトリは **既に inventory に存在しない** ことを確認 (backend/tests/migrations/test_archive_legacy_twins.py::test_legacy_router_file_absent / test_legacy_models_directory_absent). FastAPI 標準動作で未登録 path は 404. 410 専用 stub router は ADR-015 Decision 5 で不採用 (新規 surface area を増やさない方針). client は modern endpoint (`/api/bf_tasks` / `/api/ai_employees` / `/api/prs` / `/api/github_repos`) に切替えること.
- [x] AC-F3: EVENT-DRIVEN: When `bash scripts/lint-mock.sh` rule #3 (archive-residue) runs, the system shall find 0 residue (the 4 legacy router files and model directory are fully removed from source).
  - impl: scripts/lint-mock.sh:185-202 — rule #3 に T-V3-D-04 legacy 4 router file + backend/app/models/legacy/ 検査を追加. `legacy_routers` 配列 (185-190) + 該当 file 存在で `WARN` + `EXIT_CODE=1`. 現状すべて不在で `OK: ARCHIVE 残留なし` を返す.
- [x] AC-F4: UNWANTED: If any active foreign key still references the 4 legacy tables, the migration shall raise relation-error and shall not commit until all FKs are remapped.
  - impl: supabase/migrations/20260516140000_archive_legacy_twins.sql:40-91 (pre guard) + 100-138 (pr_comments.pr_id repoint: legacy pull_requests → modern prs) + 145-173 (post guard). `external_fk_count > 0` → `RAISE EXCEPTION ... USING ERRCODE = 'feature_not_supported'` で transaction abort.

## Tier 3: Regression

CI gate を逐語コピー. 各 AC に実行ログ (exit code / 件数) を埋める.

- [x] AC-R1: The system shall pass `pytest backend/tests/migrations/test_archive_legacy_twins.py -v` with 0 failures.
  - 実行ログ: pytest exit 0 / 17 collected / 17 passed (詳細: 4 rename pair + DROP guard + ALTER syntax + idempotent + 4 router file absent + models dir absent + lint rule + pre/post guard + pr_comments repoint + format(%I) safe + COMMENT marker + RLS service_role only × 4 + header mapping × 4). 実行コマンド: `cd backend && pytest tests/migrations/test_archive_legacy_twins.py -v`.
- [x] AC-R2: The system shall pass `bash scripts/lint-mock.sh` rule #3 (archive-residue) with 0 residue.
  - 実行ログ: `bash scripts/lint-mock.sh` rule #3 → `OK: ARCHIVE 残留なし` (onlook/penpot 不在 + legacy_*.py 4 file 不在 + backend/app/models/legacy/ 不在). exit code 0 (rule #3 単体). 他 rule との合算 exit は別 task が触る範囲外.
- [x] AC-R3: The system shall pass `python3 scripts/validate-tickets.py --check-file docs/task-decomposition/2026-05-16_v3_phase1/tickets-group-d-drift.json` for entry T-V3-D-04.
  - 実行ログ: `python3 scripts/validate-tickets.py --check-file docs/task-decomposition/2026-05-16_v3_phase1/tickets-group-d-drift.json` → T-V3-D-04 entry 既存 schema (EARS AC functional 4 + regression 4 + label=ARCHIVE + branch=claude/T-V3-D-04 + audit_md_path 一致) を pass. (本 task では tickets json には触らないため drift fix scope 内で entry は不変)
- [x] AC-R4: The system shall pass `bash scripts/audit-md-check.sh T-V3-D-04`.
  - 実行ログ: `bash scripts/audit-md-check.sh T-V3-D-04` → Tier 1 / 2 / 3 セクション 3 個揃い、generic phrase regex `shall implement T-[A-Z0-9-]+ as specified` は不出現 (具体 impl/log を全 AC に記載). exit code 0.

## 破壊的変更 / 警告

- ⚠ destructive: legacy table rename, data preserved via _archived_ prefix
- ⚠ destructive (FK remap): pr_comments.pr_id の FK は legacy `pull_requests(id)` から modern `prs(id)` に repoint される (新 v3 task で `prs` 行と関連付ける意図のため意味的にも正しい). 旧 production で pr_comments に legacy pull_requests を参照する row があれば事前 data migration 必要 (現時点で 0 row 想定).
- ロールバック: ADR-015 末尾「ロールバック」セクション参照. `ALTER TABLE _archived_<name> RENAME TO <name>` で逆向き rename 可能.

## 着手記録
- 着手日: 2026-05-17
- 担当 session: agent-a6a6666b9ea9558a5 (worktree /home/user/Build-Factory/.claude/worktrees/agent-a6a6666b9ea9558a5)
- branch: `claude/T-V3-D-04-r2`

## 完了記録
- 完了日: 2026-05-17
- Decision: IMPL_DONE → PR open 後 reviewer 確認待ち
- PR: TBD (subagent が PR 作成後に追記)

## ノート
- 本 task は drift fix Wave 4 の一部. 上流 drift summary を読み 1 entity / 1 endpoint / 1 screen 単位で進める.
- audit MD validator (`scripts/audit-md-check.sh`) は内容の薄い汎用フレーズを reject するため、AC ごとに具体的な impl/log 内容を必ず記入する.
- 完了報告では `bash scripts/audit-md-check.sh T-V3-D-04` exit 0 を必須とする.
- 静的 SQL 検証なので production DB 接続なしで pytest 実行可能 (CI で alembic upgrade head は別 gate).
- legacy 4 router file (backend/routers/legacy_*.py) と backend/app/models/legacy/ は本 task 着手前から既に inventory 上に存在せず, 物理削除は不要. lint rule #3 拡張で「将来復活したら fail」させる予防策のみ追加.
